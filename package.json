{
  "name": "smart-web-kit",
  "version": "1.5.0",
  "description": "swr — Smart Web Read: curl → real-Chrome → network-JSON escalation ladder for AI agents with source-discovery and multi-source research skills. Zero npm dependencies. One install (`npm i -g`), one command (`swr <url>`), one honest result.",
  "bin": {
    "swr": "tools/swr"
  },
  "files": [
    "tools/",
    "skills/",
    "install.sh",
    "tests/",
    "README.md"
  ],
  "keywords": [
    "ai-agents", "claude-code", "web-scraping", "opencli", "escalation",
    "cloudflare", "spa", "markdown", "search", "cli"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
